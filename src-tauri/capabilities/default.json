{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "TDA Dashboard 기본 권한 — invoke(코어) + 자동 업데이트/재시작",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "process:default"
  ]
}
